Arquivo marcador — existe só pra dar um diff real em brasilon/** quando a
rotina automática de retry precisa forçar um novo deploy do Brasil ON
(deploy-brasilon.yml só dispara em push que toque brasilon/**; não é
possível disparar via workflow_dispatch nesta integração — 403). Ver
brasilon/CLAUDE.md, seção "Nota técnica". Conteúdo sem função nenhuma além
de mudar — não apagar, é usado pela rotina de retry da cota da Vercel.

last_retry_utc: 2026-08-27T06:26:00Z
