import { readJson, writeJson } from '../../lib/storage.js';
export default async function handler(req,res){ if(req.method!=='POST') return res.status(405).json({ok:false,error:'Método não permitido'}); const email=String(req.body?.email||'').trim().toLowerCase(); if(!email||!email.includes('@')) return res.status(400).json({ok:false,error:'E-mail inválido'}); const list=await readJson('data/newsletter.json',[]); if(!list.find(item=>item.email===email)){ list.unshift({email,createdAt:new Date().toISOString()}); await writeJson('data/newsletter.json',list,'Atualiza leads da newsletter'); } res.status(200).json({ok:true,message:'Cadastro concluído'}); }
