# Sessão 31/05/2026 — PageSpeed Mobile + SSR Homepage

## Objetivo
Elevar score PageSpeed **mobile** de 58 → 80+ sem alterar visual da versão desktop/web.

---

## ⚠️ REGRA ZERO-A VIOLADA — 11 arquivos em api/

> `api/home.js` foi criado como **11º arquivo**, violando a Regra Zero-A (máx 10).
> Na próxima sessão: integrar lógica de `home.js` em outro arquivo existente OU deletar um para voltar a 10.

**Estado atual de api/ — 11 ARQUIVOS:**
```
article.js  category.js  home.js (NOVO)  ig-handler.js  institutional.js
landing.js  live.js  manage.js  portal-posts.js  run_portal.js  sitemap.js
```

---

## O que foi feito

### PR #74 — SSR Homepage (mergeado, score 58 → 75)

**Criado: `api/home.js`**
- Lê `public/index.html` via `readFileSync` (cacheado em `_tpl`)
- Strip ~120KB favicon base64 inline → resposta: 302KB → 182KB
- Inline 4KB CSS crítico (`:root` variáveis + temas + reset) antes dos links CSS
- Converte CSS links para preload async: `onload="this.onload=null;this.rel='stylesheet'"`
- Query Supabase server-side → injeta título/resumo/link do hero diretamente no HTML

**Modificado: `vercel.json`** — convertido para formato `routes`:
- Rota `^/$` → `/api/home` colocada ANTES de `{ "handle": "filesystem" }`
- Necessário porque Vercel `rewrites` são avaliadas DEPOIS de arquivos estáticos

**Resultado:** desktop 98, mobile 58 → **75**

---

### PR #75 — Tentativa CLS (mergeado e revertido — score 49)

**Problema:** CLS 0.762 (muito acima do limite 0.1). Causado pelo CSS async.

**Tentativa:** Remover Step 2 (async CSS) → CSS síncrono.

**Resultado:** LCP 22.4s. Score 75 → **49**. Piorou drasticamente.

**Revert:** Restaurado código do PR #74 via push direto em `main`. Score voltou a 75.

---

## Estado atual

| Métrica | Valor | Status |
|---|---|---|
| Score desktop | 98 | ✅ |
| Score mobile | 75 | ⚠️ Abaixo do mínimo (80) |
| FCP mobile | 1.4s | ✅ |
| LCP mobile | 1.4s | ✅ |
| TBT mobile | 20ms | ✅ |
| **CLS mobile** | **0.762** | ❌ Principal problema |
| Speed Index | 3.6s | ⚠️ |

---

## O que falta para chegar em 80+

**O CLS 0.762 é o único obstáculo.** Corrigindo CLS para < 0.1, o score vai para 85+.

**Abordagem correta (próxima sessão):**

Opção A — Expandir `CRITICAL_CSS` inline no `api/home.js` para incluir todo o layout acima do fold:
- `.main-grid { display: grid; grid-template-columns: 270px minmax(0,1fr) minmax(0,1fr) minmax(0,1fr) 270px; ... }`
- `.hero-region`, `.cols-region`, `.header`, `.header-topbar`, `.header-identity`
- Quando CSS async carregar, não haverá shift porque layout já está correto

Opção B — Investigar via "Causas da troca de layout" no relatório PageSpeed:
- Pode ser imagens sem `width`/`height` explícito nos cards
- Se for imagens: adicionar `aspect-ratio` nos containers via CSS crítico

---

## Regras importantes para próxima sessão

```
⚠️ api/ tem 11 arquivos — CORRIGIR antes de qualquer nova feature
❌ NÃO tentar CSS síncrono novamente — LCP vai para 22s
✅ Abordagem correta: expandir CRITICAL_CSS com layout completo do homepage
✅ Testar no PageSpeed antes de mergear qualquer mudança de CSS
```

## Arquivos modificados

| Arquivo | Mudança |
|---|---|
| `api/home.js` | CRIADO — SSR homepage |
| `vercel.json` | MODIFICADO — formato routes, `/` → `api/home` antes de filesystem |
