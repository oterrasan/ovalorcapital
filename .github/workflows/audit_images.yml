name: Varredura de imagens — pausado

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow pausado
        run: echo "Workflow legado pausado. Auditoria de imagens nao deve alterar posts automaticamente."
