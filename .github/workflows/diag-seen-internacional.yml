name: DIAG once - seen links internacional
on:
  push:
    branches: [diag/internacional-seen]
    paths:
      - '.github/workflows/diag-seen-internacional.yml'

jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - name: Confirmar via API pública que os 2 artigos aparecem em /internacional/
        run: |
          curl -s "https://www.ovalorcapital.com.br/api/portal-posts?recentes=true&categoria=internacional&limit=5"
          echo ""
