name: List DB Tables — pausado

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow pausado
        run: echo "Workflow legado pausado. Inspecao sensivel de banco desativada."
