name: Reset Posts — pausado

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow pausado
        run: echo "Workflow legado pausado. Reset de posts desativado para evitar alteracoes em massa."
