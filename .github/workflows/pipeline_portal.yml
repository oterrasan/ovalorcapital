# REMOVIDO — substituido por pipeline-cron.yml (a cada 5min)
# Este arquivo mantido vazio para evitar erro de sintaxe no Git
name: _removido
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo noop
