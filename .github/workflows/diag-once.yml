name: Diagnóstico único (placeholder — inerte até a próxima sessão precisar)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Nada a fazer
        run: echo "placeholder inerte — preencher quando necessário"
