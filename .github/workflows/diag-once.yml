name: Diagnóstico único (placeholder)

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/diag-once.yml'

jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - name: Checar se posts.video_url existe de verdade no banco
        run: |
          echo "--- tentando SELECT video_url em posts (limit 1) ---"
          curl -sS "https://yntwvfcxjardzafdqanj.supabase.co/rest/v1/posts?select=id,video_url&limit=1" \
            -H "apikey: $SB_KEY" -H "Authorization: Bearer $SB_KEY" \
            -w "\nHTTP_STATUS:%{http_code}\n"
          echo ""
          echo "--- reels_config do OVC (action=reels_publish sem token, só pra ver mensagem de erro) ---"
          curl -sS "https://www.ovalorcapital.com.br/api/manage" \
            -X POST -H "Content-Type: application/json" \
            -d '{"action":"reels_publish"}' \
            -w "\nHTTP_STATUS:%{http_code}\n"
          echo ""
        env:
          SB_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InludHd2ZmN4amFyZHphZmRxYW5qIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4MDM1NTMwMywiZXhwIjoyMDk1OTMxMzAzfQ.BX1N_0wHoICwK5V8-96KXaMMbA8tQManVelxS1-pO40"
