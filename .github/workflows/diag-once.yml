name: Diagnóstico único (placeholder — inerte até a próxima sessão precisar)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "placeholder inerte — brasilon.vercel.app no ar, domínio anexado ao projeto, aguardando Roberto configurar DNS na Locaweb"
