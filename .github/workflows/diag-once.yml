name: Diagnóstico único (placeholder — inerte até a próxima sessão precisar)
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/diag-once.yml'
jobs:
  diag:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Placeholder (nada a fazer)
        run: echo "diag-once.yml resetado — inerte ate a proxima sessao precisar"
