name: Diag Once
# Placeholder inerte — sem função ativa. Reutilizado sob demanda em sessões de
# trabalho para diagnósticos pontuais via GitHub Actions (que tem rede real,
# diferente do sandbox de execução), sempre resetado a este estado depois de
# usado. Nunca deve virar um workflow permanente com lógica real.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "placeholder — sem função ativa"
