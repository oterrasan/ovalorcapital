name: diag-once (placeholder — nenhum diagnóstico ativo agora)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "placeholder — edite este arquivo quando precisar rodar um diagnóstico real"
