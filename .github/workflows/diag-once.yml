name: Diagnóstico único — placeholder

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: [".github/workflows/diag-once.yml"]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder — reescrever conteúdo aqui na próxima investigação one-off
        run: |
          echo "Sem diagnóstico ativo no momento. Editar este arquivo com o próximo teste one-off quando necessário."
