name: Diagnóstico único (placeholder — inerte até a próxima sessão precisar)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "placeholder inerte — obrasilon.com.br DNS propagado, servindo via HTTP; aguardando cert HTTPS da Vercel provisionar"
