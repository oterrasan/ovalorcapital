name: Diagnóstico único (placeholder — inerte até a próxima sessão precisar)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: noop
        run: |
          echo "placeholder inerte - sync Brasil ON confirmado funcionando (112 posts sincronizados: 67 brasil-on + 45 futebol)"
