name: Keep Alive — pausado

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow pausado
        run: echo "Keepalive com commit automatico pausado para evitar deploys inesperados."
