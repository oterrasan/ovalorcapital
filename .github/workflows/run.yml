name: OVC Pipeline

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Executar pipeline OVC
        run: |
          curl -s -X POST https://www.ovalorcapital.com.br/api/run \
            -H "Content-Type: application/json"
