name: Pipeline OVC — pausado temporariamente

on:
  workflow_dispatch:

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Pipeline pausado
        run: |
          echo "Pipeline automatico pausado temporariamente. Nao gerar materias ate nova validacao editorial."
