name: Setup Categories — pausado

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow pausado
        run: echo "Workflow legado pausado. Alteracoes em categorias devem passar por revisao explicita."
