name: Setup Supabase — pausado

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow pausado
        run: echo "Workflow legado pausado. Setup/seed de banco deve ser feito somente com revisao explicita."
